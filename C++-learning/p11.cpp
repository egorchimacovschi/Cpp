#include <iostream>
using namespace std;

int main (){
    int car [3][3];//multidimensional arrays

    int rows = sizeof (car) / sizeof(car[0]);
    int columns = sizeof (car[0]) / sizeof(car[0][0]);
    return 0;
}